auth.json
*.egg-info
*.db
.coverage
.vscode/
__pycache__/
*.pyc
.venv/
sys.stderr
# Per-developer Claude Code permissions; shared config belongs in settings.json
/.claude/settings.local.json

# roborev snapshots
/.roborev/
# local roborev config
/.roborev.toml

.kata.local.toml

# setuptools build tree. Ignored here as well as in most global
# gitignores, because a stale build/lib can silently inject a deleted file
# into a freshly built wheel.
build/
dist/
*.egg-info/
